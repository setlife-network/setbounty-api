# import User, Bounty, Issue from "../generated/prisma.graphql"

type Query {
    hello(name: String): String!
}

type Mutation {
    signup(login: String): AuthPayload!
}

type AuthPayload {
    token: String
    user: User
}